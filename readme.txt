
文件已上传至github 后来又发生了更改
更新：
git add 文件名
git commit -m "注释"
git pull origin master
若出现please enter a commit message…按esc然后输入 :wq
git push -u origin master


删除：
git --help
git pull origin master  //将远程仓库里面的项目拉下来
dir          //查看有哪些文件夹
git rm -r --cached   文件名         //删除
git commit -m "注释"    //删除了文件
git push -u origin master    //推到master分支


上传
git init 
git remote add origin  https://github.com/2229157983/store
git  pull origin master
git add 文件名
git commit -m "注释"
git pull origin master
 




配置全局邮箱：git  config   --global  user.email  “2229157983@qq.com”
配置全局用户名：git  config    --global  user.name “Cxl”
